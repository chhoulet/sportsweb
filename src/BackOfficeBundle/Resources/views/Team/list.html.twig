{% extends 'BackOfficeBundle::layout.html.twig' %}

	{% block content %}

		<div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
		
					<h2> Liste des teams non-controlées :</h2>

						{% for tea in team %}

			                <h2>
			                    <a href="#">{{ tea.name }}</a>
			                </h2>
			                <p class="lead">
			                    by <a href="index.php">{{ tea.sportPracticed }}</a>
			                </p>
			                <p><span class="glyphicon glyphicon-time"></span> {{ tea.dateCreated | date('d-m-Y H:i:s')}}</p>
			                <hr>
			                <img class="img-responsive" src="http://placehold.it/900x300" alt="">
			                <hr>
			                <p>{{ tea.typeOfGame }}</p><br>
			                  <p>{{ tea.habitsOfGame }}</p><br>
			                
			                <a class="btn btn-primary" href="{{ path('back_office_admin_team_response',{id:tea.id}) }}">OK Admin <span class="glyphicon glyphicon-chevron-right"></span></a>

			                <a class="btn btn-primary" href="{{ path('back_office_team_delete',{id:tea.id}) }}">Suppression team<span class="glyphicon glyphicon-chevron-right"></span></a>

		                	<hr>

	            		{% endfor %}

	            </div>			
			</div>
		</div>
		
	{% endblock %}