{% extends 'FrontOfficeBundle::layout.html.twig' %}

	{% block content %}

		<div class="container">
            <div class="well col-lg-10">

                <h1 class="page-header">{{app.user.username}}

                    {% for sport in app.user.sportPracticed %}
                        <small>SPORT : {{ sport }}</small>
                    {% endfor %}
                        <small>AGE : {{app.user.age }} ans</small>
                        
                </h1>           

                <a href="{{ path('front_office_user_showFriends') }}">Voir mes amis</a><br><br>
                <a href="{{ path('front_office_user_invitations') }}">Voir mes invitations</a><br>
                <a href="{{ path('front_office_user_showTeams') }}">Voir mes équipes</a><br><br>

            </div>
        </div>
        <!-- /.row -->

        {# <!-- Projects Row -->
        <div class="row">

        	{% if (app.user.image == true) %}
        		{% for image in app.user.image %}
		            <div class="col-md-4 portfolio-item">
		                <a href="#">
		                    <img class="img-responsive" src="http://placehold.it/700x400" alt="">
		                </a>
		                <h3>
		                    <a href="#">{{ app.user.imageName }}</a>
		                </h3>
		                <p>{{ app.user.imageComment }}</p>
		            </div>
		        {% endfor %}
        	{% endif %}

            <div class="col-md-4 portfolio-item">              
                <h3>
                    <a href="#">Sports d'intérêt :</a>
                </h3>
                <p>{{ app.user.sportViewed}}</p>
            </div>
            
        </div>
        <!-- /.row --> #}

	{% endblock %}