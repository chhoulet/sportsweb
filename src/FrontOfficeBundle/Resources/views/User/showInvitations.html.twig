{% extends 'FrontOfficeBundle::layout.html.twig' %}

	{% block content %}

        <div class="container">
            <div class="well col-lg-10">

            {# {% if (app.user.id) isEqual(app.user.invitation.userTo) %} #}

                {% for invit in app.user.invitation %}

            		<div class="row">
                        <div class="col-lg-12">
                            <h1 class="page-header">{{ invit.content }}
                                <small>DATE : {{ invit.dateInvit | date('d-m-Y H:i') }}</small><br>
                                <small>SPORT : {{ invit.sport }} </small><br>
                                <small>LIEU : {{ invit.place }} </small><br>
                                <small>MODE : {{ invit.mode }} </small><br>
                            </h1>
                        </div>
                    </div>
               
                {% endfor %}

            {# {% endif %} #}

          {#   {% if (app.user.invitation.userFrom = app.user.id) %} #}

                {% for invit in app.user.invitation %}

                    <div class="row">
                        <div class="col-lg-12">
                            <h1 class="page-header">{{ invit.content }}
                                <small>DATE : {{ invit.dateInvit | date('d-m-Y H:i') }}</small><br>
                                <small>SPORT : {{ invit.sport }}</small><br>
                                <small>LIEU : {{ invit.place }}</small><br>
                                <small>MODE : {{ invit.mode }}</small><br>
                            </h1>
                        </div>
                    </div>
               
                {% endfor %}

          {#   {% endif %}
     #}
            </div>
        </div>
        
        <!-- /.row -->

	{% endblock %}