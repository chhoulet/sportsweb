{% extends 'FrontOfficeBundle::layout.html.twig' %}

	{% block content %}

        {# {% if (app.user.id) isEqual(app.user.invitation.userTo) %} #}

            {% for invit in app.user.invitation %}

        		<div class="row">
                    <div class="col-lg-12">
                        <h1 class="page-header">{{ invit.content }}
                            <small>DATE : {{ invit.dateInvit | date('d-m-Y H:i') }}</small>
                            <small>SPORT : {{ invit.sport }} </small>
                            <small>LIEU : {{ invit.place }} </small>
                            <small>MODE : {{ invit.mode }} </small>
                        </h1>
                    </div>
                </div>
           
            {% endfor %}

        {# {% endif %} #}

      {#   {% if (app.user.invitation.userFrom = app.user.id) %} #}

            {% for invit in app.user.invitation %}

                <div class="row">
                    <div class="col-lg-12">
                        <h1 class="page-header">{{ invit.content }}
                            <small>DATE : {{ invit.dateInvit | date('d-m-Y H:i') }}</small>
                            <small>SPORT : {{ invit.sport }}</small>
                            <small>LIEU : {{ invit.place }}</small>
                            <small>MODE : {{ invit.mode }}</small>
                        </h1>
                    </div>
                </div>
           
            {% endfor %}

      {#   {% endif %}
 #}

        
        <!-- /.row -->

	{% endblock %}