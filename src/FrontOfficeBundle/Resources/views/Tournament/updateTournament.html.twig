{% extends 'FrontOfficeBundle::layout.html.twig' %}

	{% block content %}		

		<h2>{{ name }}</h2>

		{% for message in app.session.flashbag.get('update') %}
			{{ message }}
		{% endfor %}

		{{ form(form) }}

	{% endblock %}