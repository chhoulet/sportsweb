{% extends 'FrontOfficeBundle::layout.html.twig' %}

	{% block content %}

		<div class="container">
			<div class="well col-lg-6">

				{% if (userTo) %}

					<p>invite  {{ userTo.username }}</p>

				{% else %}

					<p>Lancer une invitation :</p>
	
				{% endif %}

				{{ form(form) }}

			</div>
		</div>

	{% endblock %}